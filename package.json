{
  "name": "serverless-build-client",
  "version": "2.1.0",
  "description": "A Serverless Framework plugin used to build front end applications",
  "main": "src/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tgfischer/serverless-build-client.git"
  },
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage && cat ./coverage/lcov.info | npx coveralls",
    "lint": "npx eslint --fix -- ."
  },
  "author": "Tom Fischer",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.2",
    "eslint": "^4.19.1",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-prettier": "^2.6.0",
    "jest": "^23.2.0",
    "prettier": "^1.13.5",
    "serverless": "^1.27.3"
  },
  "dependencies": {
    "cross-spawn": "^6.0.5"
  }
}
